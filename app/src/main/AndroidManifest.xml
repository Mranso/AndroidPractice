<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.anso.androidpractice">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".AnimationActivity" />
        <activity android:name=".PermissionsActivity" />
        <activity android:name=".DialogActivity">

        </activity>
        <activity android:name=".banner.ViewPagerBannerActivity" />
        <activity android:name=".ProgressActivity" />
        <activity android:name=".RefreshActivity" />
        <activity android:name=".activityturnanimation.BeforeActivity" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".activityturnanimation.AfterActivity">

        </activity>
    </application>

</manifest>